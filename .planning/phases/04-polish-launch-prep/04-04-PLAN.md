---
phase: 04-polish-launch-prep
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - web-dashboard/vercel.json
  - .vercelignore
  - mobile-app/assets/screenshots/ios/
  - mobile-app/assets/screenshots/android/
  - web-dashboard/qa-checklist.md
autonomous: false
requirements:
  - WEB-05
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - STORE-02
user_setup: []
must_haves:
  truths:
    - "Web dashboard deployed to Vercel with proper configuration"
    - "App tested on physical iOS device (iPhone)"
    - "App tested on physical Android device (multiple screen sizes)"
    - "Web dashboard tested across Chrome, Firefox, Safari browsers"
    - "Accessibility audit passed (screen readers, keyboard navigation, color contrast)"
    - "App store screenshots created for iOS and Android"
  artifacts:
    - path: "web-dashboard/vercel.json"
      provides: "Vercel deployment configuration"
      contains: "routes, headers"
    - path: "mobile-app/assets/screenshots/ios/"
      provides: "iOS App Store screenshots (6.7\" and 5.5\" iPhone)"
      min_files: 3
    - path: "mobile-app/assets/screenshots/android/"
      provides: "Android Play Store screenshots (phone and tablet)"
      min_files: 2
    - path: "web-dashboard/qa-checklist.md"
      provides: "Manual testing checklist for iOS, Android, web, accessibility"
      min_lines: 100
  key_links:
    - from: "web-dashboard/vercel.json"
      to: "Vercel deployment"
      via: "vercel.json configuration for routing and caching"
      pattern: "routes.*source.*destination"
    - from: "mobile-app/assets/screenshots/ios/"
      to: "Apple App Store Connect"
      via: "Screenshot upload for app store listing"
      pattern: "\\.png$"
    - from: "web-dashboard/qa-checklist.md"
      to: "Quality assurance testing"
      via: "Step-by-step manual testing checklist"
      pattern: "Test.*|Verify.*|Check.*"
</must_haves>
---

<objective>
Finalize Vercel deployment configuration for web dashboard, create app store screenshots for iOS and Android, and execute comprehensive quality assurance testing across physical devices (iOS, Android) and browsers (Chrome, Firefox, Safari) including accessibility audit.

Purpose: Ensure web dashboard is production-deployed, app store screenshots professionally created, and all platforms manually tested before app store submission.
Output: Vercel deployment configured, app store screenshots (iOS: 3+, Android: 2+), QA checklist completed with all tests passed.
</objective>

<execution_context>
@C:/Users/PRAVEEN/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/PRAVEEN/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-launch-prep/04-RESEARCH.md

# Previous Phase Context
@.planning/phases/02-core-mobile-app/02-02-IMPLEMENTATION-PLAN-SUMMARY.md
@.planning/phases/03-web-dashboard/03-IMPLEMENTATION-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Vercel Deployment Configuration</name>
  <files>
    web-dashboard/vercel.json
    .vercelignore
    web-dashboard/ai-model-radar.html
  </files>
  <read_first>
    1. web-dashboard/vercel.json - to see current Vercel configuration
    2. web-dashboard/ai-model-radar.html - to understand dashboard structure
  </read_first>
  <action>
    Update Vercel deployment configuration (web-dashboard/vercel.json):

    Ensure vercel.json exists with optimized configuration:
    ```json
    {
      "version": 2,
      "builds": [
        {
          "src": "ai-model-radar.html",
          "use": "@vercel/static"
        }
      ],
      "routes": [
        {
          "src": "/(.*)",
          "dest": "/ai-model-radar.html",
          "status": 200
        }
      ],
      "headers": [
        {
          "source": "/(.*)",
          "headers": [
            {
              "key": "X-Content-Type-Options",
              "value": "nosniff"
            },
            {
              "key": "X-Frame-Options",
              "value": "SAMEORIGIN"
            },
            {
              "key": "X-XSS-Protection",
              "value": "1; mode=block"
            },
            {
              "key": "Referrer-Policy",
              "value": "strict-origin-when-cross-origin"
            }
          ]
        }
      ],
      "redirects": [
        {
          "source": "/index.html",
          "destination": "/",
          "statusCode": 301
        }
      ],
      "rewrites": [
        {
          "source": "/data/(.*)",
          "destination": "https://raw.githubusercontent.com/PraveenJayaprakash-JP/AI-Model-Radar/main/data/$1"
        }
      ]
    }
    ```

    Create .vercelignore in project root:
    ```gitignore
    # Git and repository files
    .git
    .gitignore
    .planning

    # Node modules
    node_modules/

    # Environment files
    .env
    .env.local
    .env.production

    # Development files
    .DS_Store
    Thumbs.db
    *.log

    # Mobile app (not deployed to Vercel)
    mobile-app/

    # Scraper (not deployed to Vercel)
    scraper/

    # Test files
    tests/
    *.test.*
    __tests__/

    # Documentation (can be deployed but optional)
    README.md
    *.md
    ```

    Verify dashboard fetches from correct production URL:
    In web-dashboard/ai-model-radar.html, verify DATA_URL variable:
    ```javascript
    const DATA_URL = 'https://raw.githubusercontent.com/PraveenJayaprakash-JP/AI-Model-Radar/main/data/models.json';
    ```

    Test Vercel deployment locally (optional):
    ```bash
    cd web-dashboard
    npx vercel dev
    ```
    Visit http://localhost:3000 to verify dashboard loads.

    Redeploy to Vercel with updated configuration:
    ```bash
    cd web-dashboard
    npx vercel --prod
    ```

    Verify deployment at https://web-dashboard-omega-cyan.vercel.app

    Connect GitHub repo to Vercel for auto-deploy (if not already):
    1. Go to Vercel dashboard → Project Settings → Git
    2. Click "Connect Repository"
    3. Select: PraveenJayaprakash-JP/AI-Model-Radar
    4. Configure:
       - Root Directory: web-dashboard
       - Build Command: (empty, static hosting)
       - Output Directory: ./
       - Branch: main
    5. Click "Deploy"
  </action>
  <verify>
    <automated>if (Test-Path "web-dashboard/vercel.json") { Write-Output "vercel.json exists" } else { Write-Output "vercel.json missing"; exit 1 }</automated>
  </verify>
  <done>
    vercel.json configured with routes, headers, redirects, rewrites
    .vercelignore created to exclude non-web files
    Security headers configured (X-Content-Type, X-Frame-Options, etc.)
    Data fetch URL verified (points to raw GitHub)
    Dashboard re-deployed to Vercel production
    GitHub repo connected to Vercel for auto-deploy on push to main
    Deployment accessible at https://web-dashboard-omega-cyan.vercel.app
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Vercel deployment configuration for web dashboard</what-built>
  <how-to-verify>
    1. Open web dashboard: https://web-dashboard-omega-cyan.vercel.app
    2. Verify page loads without errors (HTTP 200, no console errors)
    3. Test all features: search, filters, responsive layout, dark mode
    4. Verify populates with real model data (if Phase 1 scraper is live)
    5. Test on different devices: mobile phone, tablet, desktop
    6. Verify Core Web Vitals: LCP < 2.5s, FID < 100ms (use Lighthouse DevTools)
    7. Test offline mode: disconnect network, verify cached data loads
  </how-to-verify>
  <resume-signal>Type "web-deployment-approved" after verifying all web dashboard features work correctly in production, or describe issues to fix.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create App Store Screenshots</name>
  <files>
    mobile-app/assets/screenshots/ios/
    mobile-app/assets/screenshots/android/
  </files>
  <read_first>
    N/A - human task to capture screenshots on physical devices
  </read_first>
  <action>
    Create app store screenshots directory structure:

    iOS Screenshots (Apple App Store):
    - Create directory: `mobile-app/assets/screenshots/ios/`
    - Capture screenshots on iPhone (physical device or simulator):
      - 1. Browse tab showing model list with search bar
      - 2. Model card detail showing pricing and capabilities
      - 3. Compare tab showing side-by-side model comparison (if implemented) or Discover tab
      - 4. (Optional) Dark mode version of any screen
    - Screenshots must be:
      - Size: 1242x2688 (6.7" iPhone) or 1242x2208 (5.5" iPhone)
      - Format: PNG (lossless)
      - No UI chrome (status bar, home indicator acceptable)
      - Show core features: search, pricing, free tier badges
    - Save as:
      - ios/iphone-65-browse.png (Browse tab)
      - ios/iphone-65-model-detail.png (Model card)
      - ios/iphone-65-compare.png (Compare or Discover tab)
      - (Optional) ios/iphone-65-dark-mode.png (Dark mode)

    Android Screenshots (Google Play Store):
    - Create directory: `mobile-app/assets/screenshots/android/`
    - Capture screenshots on Android device:
      - 1. Phone 4K (3840x2160) or Phone 1080p (1920x1080) - Browse tab
      - 2. Phone - Model card or Compare tab
      - 3. (Optional) 7" Tablet - Browse tab showing responsive layout
    - Screenshots must be:
      - Minimum size: 320px (width), 320px (height)
      - Recommended: 1080x1920 (phone) or 1920x1200 (tablet)
      - Format: JPG or PNG (JPG preferred for smaller file size)
      - No status bar or navigation bar (use screenshot tool that crops UI chrome)
    - Save as:
      - android/phone-browse.png (Browse tab on phone)
      - android/phone-model-detail.png (Model card on phone)
      - (Optional) android/tablet-browse.png (Browse tab on tablet)

    Screenshot capture tools:
    - iOS: Use Xcode Simulator → Debug → Take Screenshot, or physical iPhone → Power + Vol Up button
    - Android: Use Android Device Monitor (deprecated), or scrcpy tool, or physical device → Power + Vol Down button
    - For clean screenshots (no UI chrome): Use Expo's `eas screenshot` tool (requires EAS Build setup):
      ```bash
      npx eas screenshot --platform ios --profile preview
      npx eas screenshot --platform android --profile preview
      ```

    Screenshot guidelines:
    - Use real model data (from Phase 1 scraper)
    - Show variety: search results, model detail, comparison
    - Include key features: free tier badges, pricing info, provider logos
    - Clean background (white or light gray, no distracting elements)
    - Clear text: model names, pricing, capability tags
    - For comparison screenshots, show models with different prices (free vs paid)
  </action>
  <verify>
    <automated>if (Test-Path "mobile-app/assets/screenshots/ios") { Write-Output "iOS screenshot dir exists" } else { Write-Output "iOS screenshot dir missing"; exit 1 }</automated>
  </verify>
  <done>
    iOS screenshot directory created (mobile-app/assets/screenshots/ios/)
    At least 3 iOS screenshots captured (Browse, Model Detail, Compare/Discover)
    Screenshots meet Apple size requirements (1242x2688 or 1242x2208)
    Screenshots show core features (search, pricing, free tier)
    Android screenshot directory created (mobile-app/assets/screenshots/android/)
    At least 2 Android screenshots captured (phone Browse, phone Model Detail)
    Optional: Tablet screenshot for Android
    All screenshots uploaded to mobile-app/assets/screenshots/
    Screenshots ready for app store submission (will upload to App Store Connect / Google Play Console)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>App store screenshots for iOS and Android</what-built>
  <how-to-verify>
    Review screenshots in mobile-app/assets/screenshots/:

    iOS Screenshots:
    1. Browse screen showing model list with search bar and filters
    2. Model detail showing pricing, capabilities, provider logo
    3. Compare or Discover screen showing key features
    4. (Optional) Dark mode screenshot
    Verify: clear text, readable pricing info, free tier badges visible

    Android Screenshots:
    1. Phone Browse screen with model list
    2. Phone Model Detail or Compare screen
    3. (Optional) Tablet Browse screen
    Verify: responsive layout, clear text, pricing info visible

    Both platforms:
    - Screenshots show real data (not placeholder)
    - Images are high quality (no blurring, UI chrome removed if possible)
    - Key features visible: search, filters, pricing, free tiers
    - App name "AI Model Radar" appears on browse screens
  </how-to-verify>
  <resume-signal>Type "screenshots-approved" after reviewing all screenshots and confirming they meet app store guidelines, or describe issues to fix.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Manual QA Testing Checklist</name>
  <files>
    web-dashboard/qa-checklist.md
    mobile-app/qa-mobile-checklist.md
  </files>
  <read_first>
    N/A - creating new QA checklist documents
  </read_first>
  <action>
    Create comprehensive QA checklist for web dashboard (web-dashboard/qa-checklist.md):

    ```markdown
    # Web Dashboard QA Checklist
    **Date:** May 8, 2026
    **URL:** https://web-dashboard-omega-cyan.vercel.app

    ## Cross-Browser Testing

    ### Chrome (Desktop)
    - [ ] Page loads successfully (HTTP 200)
    - [ ] Search bar filters models in real-time (~200ms response)
    - [ ] Provider filter toggles work (multi-select)
    - [ ] Responsive layout adapts: 3-column desktop, 2-column tablet, 1-column mobile
    - [ ] Dark mode respects system preference
    - [ ] Model cards render correctly (provider logo, name, pricing)
    - [ ] Free tier badges display in orange accent
    - [ ] "Cheapest for X tokens" calculator works (if implemented)
    - [ ] Offline mode: disconnect network, verify cached data loads
    - [ ] Console: no JavaScript errors (DevTools Console)
    - [ ] Performance: LCP < 2.5s, FID < 100ms (Lighthouse)

    ### Firefox (Desktop)
    - [ ] Page loads successfully
    - [ ] Search and filters work identically to Chrome
    - [ ] Responsive layout adapts correctly
    - [ ] Dark mode respects system preference
    - [ ] No console errors (Firefox DevTools Console)
    - [ ] Performance similar to Chrome (within 10%)

    ### Safari (Desktop - Mac only)
    - [ ] Page loads successfully
    - [ ] Search and filters work
    - [ ] Responsive layout adapts correctly
    - [ ] Dark mode respects system preference
    - [ ] No console errors (Safari Web Inspector)

    ### Safari (iOS - iPhone)
    - [ ] Page loads on mobile Safari
    - [ ] 1-column responsive layout (not 3-column)
    - [ ] Touch targets ≥ 44x44px (iOS spec)
    - [ ] Search bar focuses on load (autofocus)
    - [ ] Filter chips are tappable with finger
    - [ ] Pull-to-refresh not needed (static page, but verify scroll works)
    - [ ] Keyboard dismissal works after search
    - [ ] Offline mode works (disconnect WiFi/Cellular, verify cache loads)

    ### Chrome (Android - Mobile)
    - [ ] Page loads on mobile Chrome
    - [ ] 1-column responsive layout
    - [ ] Touch targets ≥ 48x48px (Android spec)
    - [ ] Search bar works with virtual keyboard
    - [ ] Filter chips tappable
    - [ ] Back button navigation returns to previous page (if using router)

    ## Feature Verification

    ### Search Functionality
    - [ ] Search box filters by model name (e.g., "GPT" returns GPT models)
    - [ ] Search filters by provider (e.g., "OpenAI" returns OpenAI models)
    - [ ] Search case-insensitive (returns results for "gpt" or "GPT")
    - [ ] Search debounced (~200ms delay, no flickering)
    - [ ] Empty search shows all models
    - [ ] No search results found shows message

    ### Provider Filters
    - [ ] Provider filter chips toggle on/off
    - [ ] Multi-select: can select OpenAI AND Anthropic
    - [ ] "Clear filters" button resets all filters
    - [ ] Filter results update immediately (no page reload)

    ### Model Cards
    - [ ] Provider logos render correctly (OpenAI, Anthropic, Google, etc.)
    - [ ] Model names display correctly
    - [ ] Pricing shows input/output cost per 1K tokens
    - [ ] Free tier badges show "FREE" with limit (e.g., "10K req/mo")
    - [ ] Free tier badges are orange accent color
    - [ ] Capability tags display (text, vision, audio, etc.)
    - [ ] Newest models highlighted (if implemented)

    ### Responsive Design
    - [ ] Desktop (> 1024px): 3-column grid
    - [ ] Tablet (768px - 1024px): 2-column grid
    - [ ] Mobile (< 768px): 1-column grid
    - [ ] No horizontal scroll on mobile
    - [ ] Images scale properly (no pixelation or overflow)
    - [ ] Text readability maintained at all sizes

    ### Dark Mode
    - [ ] Respects system dark mode preference (CSS `@media (prefers-color-scheme: dark)`)
    - [ ] Manual dark mode toggle (if implemented)
    - [ ] High contrast in both light and dark modes
    - [ ] Color contrast ratio ≥ 4.5:1 for text (Lighthouse accessibility audit)

    ### Offline Mode
    - [ ] First visit populates localStorage with model data
    - [ ] Subsequent visits load from localStorage (if no network)
    - [ ] "Offline mode" indicator shows when offline
    - [ ] Data refreshes when network reconnects

    ## Accessibility Audit

    ### Keyboard Navigation
    - [ ] Tab key focuses search box on load (autofocus)
    - [ ] Tab navigates through interactable elements (search, filter chips)
    - [ ] Enter key searches in search box
    - [ ] Space/Enter toggles filter chips
    - [ ] All interactive elements have visible focus indicator

    ### Screen Reader Support
    - [ ] (Mac) VoiceOver announces model names, pricing, provider
    - [ ] (Windows) NVDA or JAWS reads Alt text for provider logos
    - [ ] Semantic HTML used (proper headings, lists, buttons)
    - [ ] ARIA labels on icons (e.g., "Search models", "Filter by OpenAI")

    ### Color Contrast
    - [ ] Lighthouse accessibility audit passes (score ≥ 90)
    - [ ] Text compared to background has contrast ≥ 4.5:1
    - [ ] Reserve colors for links and interactive elements
    - [ ] Error messages have high contrast (red on white/red on dark)

    ### Screen Size Zoom
    - [ ] Content readable at 200% zoom (WCAG 2.1 AA)
    - [ ] No horizontal scroll at 150% zoom
    - [ ] Text reflows at 200% zoom (maintains readability)

    ## Performance Verification

    ### Core Web Vitals
    - [ ] LCP (Largest Contentful Paint) < 2.5s
    - [ ] FID (First Input Delay) < 100ms
    - [ ] CLS (Cumulative Layout Shift) < 0.1
    - [ ] Lighthouse Performance score ≥ 90

    ### Load Time
    - [ ] Initial load < 1.5s on desktop (Chrome DevTools Network: Clear cache, hard reload)
    - [ ] Initial load < 2.5s on mobile (3G throttling)
    - [ ] Image loading: provider logos load without blocking render
    - [ ] JavaScript bundle size < 100KB (Check Sources panel in DevTools)

    ---

    **Testing Status:**
    - Chrome (Desktop): [ ] PASS / [ ] FAIL
    - Firefox (Desktop): [ ] PASS / [ ] FAIL
    - Safari (Desktop): [ ] PASS / [ ] FAIL
    - Safari (iOS): [ ] PASS / [ ] FAIL
    - Chrome (Android): [ ] PASS / [ ] FAIL
    - Accessibility (Lighthouse): Score: ___/100
    - Performance (Lighthouse): Score: ___/100

    **Overall Pass Rate:** ___/100 (e.g., 95/100 items checked)

    **Blocking Issues:**
    1.
    2.
    3.

    **Non-Blocking Issues:**
    1.
    2.
    3.

    **Tester:** [Your Name]
    **Date:** [Date]
    ```

    Create mobile QA checklist (mobile-app/qa-mobile-checklist.md):

    ```markdown
    # Mobile App QA Checklist
    **Date:** May 8, 2026
    **Build:** Expo Preview Build (or Production Build)

    ## iOS Testing (iPhone)

    ### Basic Functionality
    - [ ] App launches within 2 seconds of icon tap
    - [ ] Splash screen displays with app icon
    - [ ] All 4 tabs load: Discover, Browse, Compare, Profile
    - [ ] Tab navigation works without lag
    - [ ] Back button returns to previous screen (if stack navigation added)
    - [ ] App doesn't crash on launch or navigation

    ### Data Loading
    - [ ] Models load from GitHub (data fetch works)
    - [ ] Initial load shows "Loading AI models..." (ActivityIndicator)
    - [ ] Background refresh shows "Updating models..." banner
    - [ ] Stale data > 15 minutes shows orange warning banner
    - [ ] Offline mode: disconnect WiFi, verify cached data loads from MMKV

    ### Search & Filters (Browse Tab)
    - [ ] Search box filters models in real-time (~200ms)
    - [ ] Search focus autofocus on Browse screen load
    - [ ] Provider filter chips toggle on/off
    - [ ] Multiple providers can be selected (multi-select)
    - [ ] Capability filters work (text, vision, audio, etc.)
    - [ ] Clear filters button resets all filters

    ### Discover Tab
    - [ ] "New Models" section shows (< 30 day launch)
    - [ ] "Free Models" section shows (free tier badge)
    - [ ] Sections scroll independently from main model list
    - [ ] Spotlight sections render ModelCards correctly

    ### Model Cards
    - [ ] Provider logo displays correctly (OpenAI, Anthropic, etc.)
    - [ ] Model name displays correctly
    - [ ] Contact window shows (e.g., "128K tokens")
    - [ ] Pricing displayed (/input cost) if paid
    - [ ] Free tier badge shows "FREE" with limit
    - [ ] Capability tags display
    - [ ] Share button on every model card (from Plan 04-01)
    - [ ] Share button opens iOS share sheet with model name/URL
    - [ ] Share to Messages, Email, Twitter works

    ### List Scrolling Performance
    - [ ] FlashList scrolls smoothly at 60fps (verify with Performance Monitor)
    - [ ] No frame drops when scrolling through 50+ models
    - [ ] Pull-to-refresh gesture works
    - [ ] Scroll position maintained during refresh
    - [ ] Overscroll bounce effect works (iOS native)

    ### Error Handling
    - [ ] Error banner displays if fetch fails
    - [ ] Retry button on error banner works
    - [ ] Error boundary catches crashes and shows "Something went wrong" UI
    - [ ] Sentry logs errors (check Sentry dashboard if DSN configured)
    - [ ] No white screen on error

    ### Accessibility (iOS)
    - [ ] VoiceOver reads model names, pricing, provider
    - [ ] Touch targets ≥ 44x44px (iOS spec)
    - [ ] Color contrast ≥ 4.5:1 (use Accessibility Inspector)
    - [ ] Keyboard navigation works with external keyboard (Tab, arrows)

    ### Dark Mode
    - [ ] Toggle in Profile Settings switches between light/dark
    - [ ] Dark mode persists across app restarts (Zustand store)
    - [ ] High contrast in both modes
    - [ ] Text readability maintained in dark mode

    ## Android Testing (Phone)

    ### Basic Functionality
    - [ ] App launches within 2 seconds
    - [ ] Splash screen displays (or Android adaptive icon animation)
    - [ ] All 4 tabs load correctly
    - [ ] Tab navigation works without lag
    - [ ] Back button returns to previous screen
    - [ ] App doesn't crash

    ### Data Loading
    - [ ] Models load from GitHub
    - [ ] Initial load shows ActivityIndicator
    - [ ] Background refresh shows banner
    - [ ] Offline mode works (disconnect WiFi, verify MMKV cache)

    ### Search & Filters (Browse Tab)
    - [ ] Search box works with virtual keyboard
    - [ ] Keyboard dismisses after search
    - [ ] Provider filter chips switch on/off (toggle)
    - [ ] Multiple providers selectable
    - [ ] Capability filters work

    ### Model Cards & Sharing
    - [ ] Provider logos display
    - [ ] Share button exists on model cards
    - [ ] Share button opens Android share menu (Intent.ACTION_SEND)
    - [ ] Share to Gmail, Twitter, WhatsApp works

    ### List Scrolling Performance
    - [ ] FlashList scrolls smoothly
    - [ ] No frame drops (verify with Flipper Performance Monitor)
    - [ ] Pull-to-refresh gesture works (RTL support?)
    - [ ] Scroll position maintained

    ### Accessibility (Android)
    - [ ] TalkBack reads model names, pricing, provider
    - [ ] Touch targets ≥ 48x48px (Android spec)
    - [ ] Color contrast ≥ 4.5:1 (verify with Accessibility Scanner)
    - [ ] Keyboard navigation works (Tab, arrows, Enter)

    ### Responsive Design (Multiple Screen Sizes)
    - [ ] Phone (360x640 - common size): Model cards render correctly
    - [ ] Phone (411x731 - large phone): Model cards scale appropriately
    - [ ] Tablet (800x1280): Verify if app supports landscape
    - [ ] No horizontal scroll on phone sizes

    ## Device-Specific Edge Cases

    ### Low-RAM Device (e.g., iPhone 8, Samsung Galaxy A series)
    - [ ] App launches without Out of Memory error
    - [ ] List scrolling doesn't cause jank on low-end device
    - [ ] Background refresh doesn't freeze UI
    - [ ] App handles low memory warnings gracefully (React Native warnings)

    ### Older OS Versions
    - [ ] iOS 13+ compatible (minimum iOS version in app.json)
    - [ ] Android 8.0+ compatible (minimum Android version)
    - [ ] No crashes on older OS versions

    ### Network Conditions
    - [ ] Slow 3G connection (< 1 Mbps): Models still load (timeout handled)
    - [ ] Offline mode: Cache works without network
    - [ ] Network reconnect: Auto-refresh triggers

    ---

    **Testing Status:**
    - iOS (iPhone): [ ] PASS / [ ] FAIL
    - Android (Phone): [ ] PASS / [ ] FAIL
    - Accessibility (iOS VoiceOver/Android TalkBack): [ ] PASS / [ ] FAIL
    - Performance (60fps scrolling): [ ] PASS / [ ] FAIL

    **Blocking Issues:**
    1.
    2.
    3.

    **Non-Blocking Issues:**
    1.
    2.
    3.

    **Tester:** [Your Name]
    **Date:** [Date]
    ```

    Execute QA testing:
    1. Print/download both QA checklists
    2. Open web dashboard: https://web-dashboard-omega-cyan.vercel.app
    3. Open mobile app: Run `cd mobile-app && npx expo start` and scan QR code
    4. Test web dashboard across Chrome, Firefox, Safari (desktop), iOS Safari, Android Chrome
    5. Test mobile app on physical iOS device (iPhone) and Android device
    6. Run Lighthouse accessibility and performance audits on web dashboard
    7. Check mobile performance with Flipper Performance Monitor
    8. Test offline mode on both web and mobile
    9. Test accessibility: keyboard navigation, screen readers (VoiceOver, TalkBack)
    10. Mark each checkbox in QA checklists as PASS or FAIL
    11. Document blocking issues (must fix before app store submission)
    12. Document non-blocking issues (can defer to v1.1)
  </action>
  <verify>
    <automated>if (Test-Path "web-dashboard/qa-checklist.md") { Write-Output "QA checklist exists" } else { Write-Output "QA checklist missing"; exit 1 }</automated>
  </verify>
  <done>
    web-dashboard/qa-checklist.md created with comprehensive browser testing checklist
    mobile-app/qa-mobile-checklist.md created with iOS and Android testing checklist
    Both checklists include: functional testing, accessibility testing, performance testing
    Web dashboard tested across Chrome, Firefox, Safari (desktop and mobile)
    Mobile app tested on physical iOS device and Android device
    Lighthouse accessibility audit run (score ≥ 90 required)
    Lighthouse performance audit run (score ≥ 90 required)
    Mobile scrolling performance verified (60fps target)
    Offline mode tested on both web and mobile
    Screen reader testing completed (VoiceOver on iOS, TalkBack on Android)
    Color contrast checked (ACAG 2.1 AA: 4.5:1 minimum)
    Keyboard navigation tested on web
    All blocking issues documented (none allowed before app store submission)
    Non-blocking issues documented for v1.1
    QA checklists saved for audit trail
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Review QA results and decide if app is ready for app store submission</decision>
  <context>After executing comprehensive QA testing, review both checklists to determine if all blocking issues are resolved and if app meets production quality standards.</context>
  <options>
    <option id="approve-submission">
      <name>Approve for App Store Submission</name>
      <pros>All blocking issues resolved, performance meets targets, accessibility passes audits, screenshots and metadata ready</pros>
      <cons>None - indicates app is production-ready</cons>
    </option>
    <option id="fix-blocking-issues">
      <name>Fix Blocking Issues First</name>
      <pros>Ensures app store won't reject submission due to bugs or policy violations</pros>
      <cons>Delays submission by 1-2 days (acceptable tradeoff)</cons>
    </option>
    <option id="defer-non-blocking">
      <name>Defer Non-Blocking Issues to v1.1</name>
      <pros>Launch on schedule with core features working, minor issues tracked for future release</pros>
      <cons>Non-blocking issues like nice-to-have features won't be in v1.0</cons>
    </option>
  </options>
  <resume-signal>Select: approve-submission, fix-blocking-issues, or defer-non-blocking</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web dashboard deployment | Vercel hosts static HTML file with CDN caching |
| Mobile app testing | Physical devices used for manual testing |
| QA checklist execution | User performs manual tests across devices and browsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | I | Physical device testing | accept | Device owned by user, no remote access during testing |
| T-04-13 | R | Vercel deployment (vercel.json) | accept | Static HTML hosting, no authentication, public data only |
| T-04-14 | E | Accessibility audit | accept | Lighthouse DevTools runs locally, data not transmitted |
| T-04-15 | D | QA checklist documents | mitigate | Checklists track issues, store no PII or sensitive data |
</threat_model>

<verification>
- Vercel deployment configured with vercel.json
- Security headers configured (X-Content-Type, X-Frame-Options, etc.)
- .vercelignore created to exclude non-web files
- GitHub repo connected to Vercel for auto-deploy
- App store screenshots created (iOS: 3+, Android: 2+)
- Web dashboard tested across Chrome, Firefox, Safari
- Mobile app tested on physical iOS device and Android device
- Lighthouse accessibility audit completed (score ≥ 90)
- Lighthouse performance audit completed (score ≥ 90)
- Mobile scrolling performance verified (60fps target)
- Offline mode tested on both web and mobile
- Screen reader testing completed
- Color contrast checked (4.5:1 minimum)
- Keyboard navigation tested on web
- QA checklists created and fully completed
- All blocking issues resolved (or documented as deferred)
- App ready for app store submission
</verification>

<success_criteria>
1. Web dashboard deployed to Vercel with production configuration
2. GitHub repo connected for auto-deploy on push to main
3. App store screenshots captured (iOS: 3+, Android: 2+)
4. Screenshots meet platform guidelines (size, format, content)
5. Web dashboard tested across Chrome, Firefox, Safari (desktop and mobile)
6. Mobile app tested on physical iOS device (iPhone)
7. Mobile app tested on physical Android device (phone, multiple screen sizes)
8. Lighthouse accessibility audit score ≥ 90
9. Lighthouse performance audit score ≥ 90
10. Mobile scrolling performance meets 60fps target
11. Offline mode works on both web and mobile
12. Screen reader testing passed (VoiceOver, TalkBack)
13. Color contrast ratio ≥ 4.5:1 (WCAG 2.1 AA)
14. Keyboard navigation works on web dashboard
15. QA checklists fully completed with all critical tests passing
16. No blocking issues remaining (all fixed or deferred to v1.1)
17. App store submission readiness confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-launch-prep/04-04-SUMMARY.md`
</output>
